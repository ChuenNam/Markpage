# Auto detect text files and perform LF normalization
* text=auto

# Release binaries: never let git touch line endings
*.exe binary
*.dll binary
*.pyd binary
*.pyc binary
*.zip binary

# .bat must stay CRLF + ASCII for cmd.exe on Chinese Windows
*.bat text eol=crlf
